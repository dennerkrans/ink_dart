## 0.1.0

First release.

- `StoryController`: drives an ink_dart `Story`, collects lines, tags and
  errors, chooses, saves and loads, and exposes variables as listenables.
- `StoryView`: the story's lines and choices, with replaceable builders.
- `StoryDebugView`: position, variables, visit counts, errors and an
  optional profiler report.
